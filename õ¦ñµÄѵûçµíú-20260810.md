# Titia 时序 · 交接文档（2026-08-10 凌晨）

> 生成时间：2026-08-10 01:20
> 用途：换电脑迁移继续开发 / 交接。本文档覆盖 2026-08-06 版《TITIA_项目总结.md》之后的所有最新改动与现状。

---

## 0. 一句话现状

Titia 是个人生活时间轴 PWA（iPhone 主屏全屏运行，基准 iPhone 14 Pro 393×852pt）。
最近两天完成了**导航体系重构**：底部 TabBar → 左侧抽屉 → **固定 68px 左侧主导航 + 页面内顶部横向滑动标签**；附带修复了若干 iOS PWA 适配问题。
**数据全部存在手机浏览器的 IndexedDB 里，换电脑必须走"备份/导入"迁移数据。**

---

## 1. 关键信息速查

| 项 | 值 |
|---|---|
| 项目路径 | `C:\Users\86178\WorkBuddy\2026-08-09-01-21-53\titia-project` |
| 技术栈 | React 18 + Vite 6 + TS 5 + Tailwind v4 + zustand + dexie(IndexedDB) + hash 路由 |
| **最新线上地址（正式）** | `https://dc763d1c7f1941f1b3d6de42038020c6.bj2.agentos-app.net`（`index-NNk5zk04.js`，版本戳 `20260809e`） |
| 用户旧正式地址（bj9，**仍是旧版**） | `https://9201b66be0854ef2a3da9f46357cf3cf.bj9.agentos-app.net`（`index-CQNCdQ0A.js`，戳 `20260809c`，无侧边导航） |
| 版本号 | 侧边导航/我呀页显示 `Titia 时序 · V3.0` |
| 构建命令（managed node） | `C:\Users\86178\.workbuddy\binaries\node\versions\22.22.2\node.exe node_modules/vite/bin/vite.js build` |
| 部署方式 | WorkBuddy 的 cloudstudio 部署工具（自动绑定 dc763 workspace，产物在 `dist/`） |

⚠️ **部署工具绑定 dc763（bj2 区域），无法直接推送到 bj9（bj9 是另一个 workspace，无凭据）。**
若必须更新 bj9：用 `titia-dist-latest.zip` 在 CloudStudio 控制台覆盖上传 bj9 workspace；或直接用 dc763 链接。

---

## 2. 最近改动记录（8/9 → 8/10，倒序）

### 2.1 页面内垂直次导航 → 顶部横向滑动标签（最新）
- **背景**：全局 68px 主导航 + 页面内 68px 垂直次导航两条竖排并存，内容区被挤到 257px。
- **改动**：`BookPage`（小账）/ `SpacePage`（小窝）/ `JournalPage`（时光）的左侧垂直导航全部改为**顶部横向滑动标签行**：
  - 标签行：`flex shrink-0 items-stretch gap-5 overflow-x-auto border-b border-line bg-bg px-4 pt-[calc(var(--safe-top)+12px)]`
  - 每项 = emoji 图标 + 短文案 + 激活主色下划线（`h-[2.5px]`）
  - Journal 的 ➕ 新增按钮并入标签行右侧（`ml-auto`）
- **配套**：嵌入式页 sticky 头去掉重复安全区避让（`pt-[calc(var(--safe-top)+12px)]` → `pt-3`）：`CountdownPage`、`DiaryPage` 的 **embedded 分支**；独立模式 header 保留原 safe-top。Cycle/Pet/Vault 用 EmbeddedHeader（无 safe-top）不动。
- **效果**：内容区宽度 = 屏宽 − 68px（全局主导航）= 325px。

### 2.2 主导航：抽屉式 → 固定窄版侧边栏
- `src/components/nav/SideNav.tsx` 重写：`absolute left-0 top-0 bottom-0 z-30 w-[68px] flex flex-col border-r border-line bg-bg`，`paddingTop: var(--safe-top,44px)`、`paddingBottom: max(env(safe-area-inset-bottom),10px)`。
- 5 Tab（今日/小窝/小账/时光/我呀）：图标 22px + 2 字文案，激活=左侧 3px 竖条 + `bg-primary-soft text-primary`；底部版本 `V3.0`；今日待办红点保留。
- **删除**：手势监听、遮罩、边缘细柄、X 按钮、`open/onOpenChange/enabled` props、展开动画。
- `App.tsx`：删 `navOpen`；Level 0 主 Tab 容器 `left-[68px]`（isModule 时 `left-0 opacity-0`）；`{!isModule && <SideNav active onSwitch badge />}`。
- 用 `absolute`（相对 App 壳）而非 fixed，dev 手机框内不脱框。

### 2.3 深色状态栏/背景分割修复（已部署，但用户仍见分割 → 挂起）
- `skins.ts` `captureSafeTopFloor()`：先探测 env；归零（black-translucent 导致）则按屏幕逻辑尺寸推断：灵动岛系（14 Pro 393×852 等）→ **59px**、刘海系 → 47px、老机型 → 20px。
- 其余维持：`black-translucent` + App 顶部 `bg-bg z-40` 遮罩（高 `var(--safe-top)`）+ `html/body/#root` 背景 `var(--color-bg)` + `applySkinTo` 设 `root.style.backgroundColor = p.bg`。
- ⏸️ **挂起**：用户反馈"依然存在分割"，先搁置。

### 2.4 倒数日顶部"悬浮卡片" → 通栏+吸顶（已部署，用户仍不满意 → 挂起）
- `CountdownPage` embedded 头：去掉 Tab 下方 `border-b border-line`、间距收紧（mb-3→mb-2、mt-3→mt-2、pb-4→pb-2）、不再用共享 `EmbeddedHeader`（改内联 div，避免污染周期/购物/憨憨/密码）。
- ⏸️ **挂起**：用户反馈"还是像（悬浮卡片）"，太耗时先搁置。

### 2.5 五项 UI 需求（8/9 晚，已全部完成）
1. 倒数日独立模式右下角悬浮「+」FAB 删除（顶部已有「+ 新增」）；
2. 小账「💡 快速记账提示」加 ✕ 关闭 + `localStorage('titia_book_tip_dismissed')` 持久化（所有提示必须可关闭）；
3. 时光页顶部导航改侧边导航（后又被 2.1 改为顶部横向标签）；
4. 侧边导航版本号 `v1.0 · Titia 时光记账` → `Titia 时序 · V3.0`；
5. 其他功能/数据未动。

### 2.6 左边缘右滑冲突（已修复）
- 抽屉边缘右滑同时触发系统"返回"（iOS 左边缘手势）。
- `SideNav` touchmove 改 `{ passive:false }` + `preventDefault()`；CSS `html,body,#root` 加 `overscroll-behavior-x: none`。（该手势层现已随抽屉删除，无影响）

### 2.7 底部 TabBar 系列问题（历史，已被 2.2 取代）
- iOS PWA 底部留白/异色块反复踩坑（env 安全区、Tailwind pb-[env()] 不解析、paddingBottom 白带等）→ 最终**放弃底部导航**，改抽屉 → 改固定侧边栏。经验：底部吸底导航在 iOS PWA 是泥潭，侧边/顶部布局从结构上规避。

---

## 3. 部署与缓存机制（新接手者必读）

### 3.1 两层缓存破除
- `index.html` 加 `Cache-Control: no-cache, no-store, must-revalidate` meta + Pragma/Expires；
- **版本戳**（每次发版递增字母）：`index.html` 的 `<link rel="manifest" href="/manifest.webmanifest?v=20260809e">` 与 `vite.config.ts` 的 `start_url: '/?v=20260809e'`。当前 `e`，下次发版升 `f`。
- JS/CSS 文件名带 hash（Vite 自动），index.html 更新即引用新文件。

### 3.2 Service Worker
- 真 no-op：`src/sw.ts` 仅 skipWaiting + clients.claim，**无 fetch 处理器、无 precache**；`main.tsx` 启动时 `purgeServiceWorkers()` 注销一切 SW。防止旧缓存白屏/旧包。

### 3.3 真机验证姿势（iOS PWA）
1. 删旧主屏图标 → Safari 打开链接 → 分享 →「添加到主屏幕」→ 从新图标进入；
2. 版本戳升级可让 iOS 视为全新 web clip，避开旧缓存；
3. 用户数据在 IndexedDB，删图标/重装**不丢数据**（同源）。

---

## 4. 数据迁移（换电脑 / 换手机必看）

Titia 数据全部在浏览器 **IndexedDB**（Dexie，表：`records`（日记/关系/成长）、`media`（图片）、`transactions`（账单）、`rules`/`accounts`/`categories`、`budgets`、`vaultItems`（密码箱）、`shopping`、`pets`、`countdownEvents`、`cycleRecords`、`todos`、`settings` 等）。

**换电脑步骤**：
1. **旧设备**：打开 App →「我呀」页 → 备份/导出（生成 JSON 备份文件；「我呀」页还有「⚡ 一键导入本域名备份」按钮，自动读取同源 `/titia-backup-*.json`）；
2. 把备份 JSON 传到新电脑（微信/网盘均可）；
3. **新设备**：装好项目 → 部署 → 打开 App →「我呀」页 → 导入备份 JSON；
4. 注意：`media` 表（图片，可能 7.9MB/数十条）导入时是**分批写入**的，大备份稍等即可；导入结果会区分"全部成功/部分失败"。

⚠️ 数据约束铁律（用户反复强调）：**任何改动不得破坏数据**；修改功能前先备份。

---

## 5. 挂起 / 待办任务

| 任务 | 状态 | 说明 |
|---|---|---|
| 深色状态栏/背景分割 | ⏸️ 挂起 | `--safe-top` 已按机型兜底 59px 并部署，用户真机仍见分割；下次可从「真机 Console 读 `--safe-top` 实际值 + `innerHeight`」入手 |
| 倒数日顶部通栏头 | ⏸️ 挂起 | 已去分隔线/收间距，用户仍觉得像悬浮卡片；可尝试整页重排或改交互 |
| bj9 链接更新 | ⏳ 待办 | bj9 仍是旧版；需用户在 CloudStudio 控制台上传 `titia-dist-latest.zip`，或改用 dc763 |
| 二级导航（Book 6 项/Space 5 项）横向标签手感 | 👀 待验证 | 真机验证滑动/点按是否顺手，标签间距/胶囊样式可再调 |

---

## 6. 常用命令

```bash
# 首次：装依赖
cd titia-project && npm install

# 构建（managed node）
C:\Users\86178\.workbuddy\binaries\node\versions\22.22.2\node.exe node_modules/vite/bin/vite.js build
# 产物在 dist/，新 JS 文件名 index-<hash>.js

# 开发预览（可选）
npm run dev   # 393×852 手机框

# 部署：WorkBuddy 内用 cloudstudio 部署工具选 dist/ 目录，自动绑定 dc763
```

---

## 7. 项目结构与关键文件

```
titia-project/
├─ src/
│  ├─ app/App.tsx            全局壳：固定侧边栏 + Level0/Level1 页面栈 + 状态栏遮罩(z-40) + 羽毛笔
│  ├─ app/styles/index.css   token 三层、--safe-top/--safe-bottom、html/body/#root 背景
│  ├─ components/nav/
│  │  ├─ SideNav.tsx         全局固定 68px 主导航（5 Tab + V3.0）
│  │  ├─ EmbeddedHeader.tsx  嵌入式页共用标题行（mb-3，勿改，会影响 5 个页）
│  │  └─ PageHost.tsx        页面容器
│  ├─ theme/skins.ts         换肤 + applySkinTo + syncStatusChrome（状态栏 meta + --safe-top）
│  ├─ pages/                 home/space/book/journal/mine + 各模块页（countdown/cycle/pet/vault/diary/moments/spark/...）
│  ├─ stores/                zustand 各业务域 store
│  ├─ services/              reload/backup/haptic/inputVisibility/customSkins
│  ├─ db/                    Dexie 表与类型
│  └─ main.tsx               boot（loadCustomSkins → applySkin → renderApp）+ purgeServiceWorkers
├─ index.html                状态栏 meta、缓存 meta、manifest 链接戳
├─ vite.config.ts            PWA manifest + start_url 戳
├─ public/  dist/  scripts/  cloudflare-worker/
└─ 文档：TITIA_接手手册.md / TITIA_项目总结.md(8/6) / REBUILD.md / 交接文档-20250809.md
```

**导航/状态栏相关状态机（一句话）**：
- 全局主导航 = `SideNav`（固定 68px），主 Tab 内容区 `left-[68px]`；
- 模块页（Level1，如 /pet /diary /countdown...）全屏、不显示 SideNav；
- 主 Tab 页内二级导航 = **顶部横向滑动标签**（Book/Space/Journal）；
- 嵌入式页 sticky 头统一 `pt-3`（宿主标签行负责安全区避让），独立模式 header 保留 `pt-[calc(var(--safe-top)+Npx)]`。

---

## 8. 明天继续优化的建议入口

1. **真机验证本轮导航**：删图标重装 → 三页横向标签手感 + 内容区宽度；
2. **深色状态栏**：若继续，先取真机 `--safe-top` 实际值、`innerHeight`、`matchMedia(display-mode)`，判断 59px 兜底是否生效；
3. **倒数日头部**：可尝试把「期待/足迹/时间轴」与 chips 合并为纯文字筛选（去胶囊），或接受现状；
4. **bj9 同步**：决定 dc763 转正 or 控制台上传 zip；
5. 版本戳升 `f` 后重新构建部署，避免 iOS 缓存旧包。
