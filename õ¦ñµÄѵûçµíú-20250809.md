# Titia 时序 · 任务交接文档

> 生成时间:2025-08-09
> 目的:让桌面端 WorkBuddy 读取本文档后,无缝接手本任务(背景、已做改动、部署、待办一目了然)。

---

## 一、任务背景

项目是 **Titia 时序** —— 一个"个人生活时间轴"移动端优先 Web App(React + Vite + Tailwind + PWA),记录生活痕迹、资产账本、倒计时、日记、待办等。项目在 `/workspace`,代码完整、可构建、已上线。

最近一轮工作是 **UI 细节优化**,但用户审阅后**不满意,已整体回退**,当前代码状态是 **V3.1(回退后的稳定版)**。

**重要教训:UI 改动的方向性很强,先确认再动手,不要自作主张改视觉。** 本轮 3 项优化全部被否决,原因:
- 顶部漏底处理(填底色 + 禁回弹):用户不喜欢
- TabBar 背景延伸铺满底部安全区:用户不喜欢
- 资产页净资产卡 + 折线图合并为一张连贯卡片:用户不喜欢

**结论:以上 3 项一律不要再做。用户原话:"还是回归上一版本吧,改的我更不喜欢了,以后有积分了再来研究"。**

---

## 二、项目技术栈与结构

| 项 | 值 |
|---|---|
| 名称/版本 | titia / 0.1.0 |
| 框架 | React 18 + Vite + Tailwind CSS v4(@tailwindcss/vite) |
| 路由 | react-router-dom(hash 路由) |
| 存储 | Dexie(IndexedDB,本地持久化)+ zustand(状态) |
| PWA | vite-plugin-pwa(离线可用,service worker 自动更新) |
| 其他依赖 | dayjs、lunar-javascript、xlsx、dexie-react-hooks |
| 目录 | `src/pages/{book,countdown,diary,home,etc.}`、`src/components/{base,icons,nav}`、`src/stores`、`src/db`、`src/theme` |
| E2E | `scripts/e2e/*.mjs` 共 32 个脚本,`scripts/e2e/run-all.sh` 批量执行 |

**页面路由**(hash):`#/`(首页)、`#/book`(资产账本)、`#/countdown`、`#/diary`、`#/mine`(我的/设置)等。

---

## 三、已完成的工作

### 3.1 已交付(V3.1 稳定版)
- 完整 App 功能:首页时间轴、资产账本(净资产/趋势图/账户列表)、倒计时、日记、待办、设置、主题换肤等。
- PWA 支持(可安装、离线可用)。
- 32 个 E2E 回归脚本覆盖主要页面与交互。

### 3.2 本轮被否决并已回退的改动(勿重做)
| # | 改动 | 回退状态 |
|---|---|---|
| ① | `src/app/styles/index.css`:全局 `overflow:hidden` + 根背景填色 + `overscroll-behavior-y:contain` | ✅ 已还原为 V3.1 原样 |
| ② | `src/components/nav/TabBar.tsx`:nav 背景铺满底部安全区 | ✅ 已还原为 V3.1 原样 |
| ③ | `src/pages/book/BookPage.tsx`:净资产卡与趋势图合并为连贯宽卡片(连带账户列表 `mt-4` 调整) | ✅ 已还原为 V3.1(sticky 净资产卡 + 独立趋势卡 + `gap-4` 布局) |

回退后已通过:`tsc --noEmit`、`npm run build`、E2E `v3fixcheck` + `bookcheck`。

---

## 四、部署状态

- **线上分享链接(稳定,桌面/移动浏览器均可访问):**
  `https://a149a628a3c099573.sh7.agentos-app.net`
- 部署内容:当前 `dist` 静态产物(V3.1 回退后重新构建),已验证 HTTP 200、页面标题与 manifest 正常。
- 发布方式:按**静态站点**发布(`--language static`),避开 Vite 反代 host 限制;hash 路由无需服务端回退。
- 下线方式:运行 `node /root/.codebuddy/skills/发布为应用/scripts/unpublish.js`(破坏性操作,需用户确认)。

---

## 五、测试情况

- 32 个 E2E 脚本:`scripts/e2e/run-all.sh <BASE_URL>` 批量运行。
- 本地:回退后 `v3fixcheck`、`bookcheck` 通过。
- 线上:上一轮全量回归 29 组全过(部署于同一链接)。
- 注意:`bookcheck3.mjs`/`bookcheck4.mjs`/`countdowncheck3.mjs` 等为历史回归脚本,改名不影响 run-all 的执行顺序。

---

## 六、下一步待办(供桌面端接手)

1. **暂无紧急待办** —— 当前是稳定可用的 V3.1。
2. 用户未来可能想再研究 UI 优化(等"有积分了"),届时**先列方案给用户选,再做**。
3. 如需下线线上链接,先问用户。
4. 常规维护:功能迭代后跑 `npx tsc --noEmit` → `npm run build` → `bash scripts/e2e/run-all.sh` 全量回归,通过后再考虑重新发布。

---

## 七、常用命令速查

```bash
cd /workspace
npm run dev            # 本地开发(393×852 手机框)
npx tsc --noEmit       # 类型检查
npm run build          # 构建到 dist
npx vite preview --port 4185   # 本地预览
BASE=http://127.0.0.1:4185 bash scripts/e2e/run-all.sh   # 全量本地回归
npm run e2e:live       # 对线上链接跑全量回归
```
