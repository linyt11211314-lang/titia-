# 快捷指令（Shortcuts）睡眠数据接入 · 部署说明

Aura 现已支持接收 iPhone「快捷指令」自动发送的睡眠数据，供诊断时读取。

> 本项目是**纯前端 PWA**，部署在静态托管上、**没有后端服务器**。
> 因此「接收 HTTP 请求」在纯前端语境下，实现为：**快捷指令「打开 URL」→ 页面 JS 解析 URL 参数 → 写入 IndexedDB**。
> 这是该约束下唯一可行的方案，已按此实现并通过验证。

---

## 一、快捷指令要怎么配

### ⚠️ 关键：用「打开 URL」，不要用「获取 URL 内容」

| 快捷指令动作 | 是否可用 | 原因 |
| --- | --- | --- |
| **打开 URL** | ✅ 可用 | 会真正加载网页、执行 JS，从而把数据写进 IndexedDB |
| 获取 URL 内容（GET） | ❌ 不可用 | 只拉取 HTML 文本，不执行 JS，数据不会被写入 |

### 可用的导入 URL 格式

把下面这条作为「打开 URL」的目标地址（把域名换成你的部署域名）：

```
https://70c149b4462e46cb96c24ae07de3c.bj2.agentos-app.net/?import-sleep=1&date=2026-08-11&sleepHours=7.5&sleepStart=23:30&sleepEnd=07:00
```

参数说明：

| 参数 | 必填 | 说明 |
| --- | --- | --- |
| `import-sleep` | 是 | 触发导入的开关，值任意（如 `1`） |
| `date` | 否 | 日期 `YYYY-MM-DD`，缺省为当天 |
| `sleepHours` | 是 | 睡眠总时长（小时，小数），如 `7.5`；范围 0–24 |
| `sleepStart` | 否 | 入睡时间 `HH:MM`，如 `23:30` |
| `sleepEnd` | 否 | 醒来时间 `HH:MM`，如 `07:00` |
| `source` | 否 | 固定写 `Shortcuts`（缺省也按 Shortcuts 处理） |

### 在「快捷指令」App 中拼装（示意）

1. 新建快捷指令 → 添加「**打开 URL**」动作。
2. URL 填上面的地址，其中 `sleepHours / sleepStart / sleepEnd / date` 用快捷指令的「魔法变量」从「健康」App 读取后填入（例如用「获取健康样本」→ 睡眠 → 时长/开始/结束，再格式化为参数）。
3. 把 `date` 设为「当前日期（格式化 YYYY-MM-DD）」。
4. 命名并收藏到主屏 / 自动化（如「每天 08:00 自动运行」）。

> 你也可以用「URL」+「设定变量」先把整串拼好，再交给「打开 URL」。
> 平台实测的部署域名即上方示例中的 `70c149…bj2.agentos-app.net`（与 App 同链接）。

---

## 二、数据落库与覆盖规则

- 存储位置：**IndexedDB 表 `sleep`**（主键 = `date`）。
- 同日多次导入：**后者覆盖前者**（`put` 以日期为主键）。
- 数据随 App 整体备份/导出一并带走（已加入 `backup.ts` 的 `TABLE_NAMES`）。

---

## 三、与 Aura 的联动（已实现）

1. **优先采用导入数据**：Aura 读取最新的睡眠记录。
2. **自动标记熬夜**：生成方案时，若最新睡眠 `sleepHours < 6`，自动在「关联因素」中加入「熬夜」（无需手动勾选）。
3. **报告展示**：Aura 方案页顶部「皮肤画像」卡会显示：
   > 📊 昨晚睡眠约 X 小时（来自健康 App 自动同步）

---

## 四、验证是否成功

用「打开 URL」触发后，页面会弹出轻提示：
- 成功：`已导入睡眠数据：7.5 小时（来源：Shortcuts）`
- 失败（参数非法）：`日期格式应为 YYYY-MM-DD` / `sleepHours 应为 0-24 的数字` 等。

随后进入 **Aura → 生成方案**，即可看到熬夜被自动标记、报告顶部出现睡眠行。

---

## 五、已知限制

- **不支持 POST Body**：纯前端无法读取请求体，必须走 URL 参数 +「打开 URL」。
- **`/api/import-sleep` 路径可能 404**：静态托管对未知路径不一定回退 index.html。代码同时兼容该路径，但**请优先使用 `?import-sleep=1` 的查询参数形式**（已验证可用）。
- **跨容器数据隔离**：通过 Safari 打开导入 URL 写入的 IndexedDB，与已安装的 PWA（主屏图标）**同域名 origin 共享**，数据互通；但若将来更换部署域名，旧数据不会迁移。
- 睡眠数据仅本地留存，不上传任何服务器。
