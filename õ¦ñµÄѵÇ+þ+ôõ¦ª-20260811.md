# Titia（时序）项目交接总结书

> 生成时间：2026-08-11 03:16（GMT+8）
> 适用对象：下一任接手开发者 / 本人后续续接
> 项目目录：`/c/Users/86178/WorkBuddy/2026-08-09-01-21-53/titia-project`

---

## 一、项目概述

**Titia（时序）** 是一款面向个人生活的**纯前端 PWA 应用**，数据全部存在浏览器 IndexedDB（Dexie），**无后端依赖**即可完整运行。功能覆盖记账、日程/待办、宠物成长记录、心情日记、相册、小窝、主题皮肤、打卡、备份导入等。

- 当前版本号：**V3.1**（最后一次版本号变更发生在茶讯 UI 调整轮次）
- 缓存戳（PWA）：**`?v=20260809o`**（关键，详见"部署纪律"）
- 数据库名：`titia`，Dexie 当前 schema version：**8**

> ⚠️ **历史教训（最高优先级）**：茶讯模块曾因数据只是"内置示例"被整体删除，之前数轮 UI 打磨全部沉没。**今后对任何功能动手前，必须确认其底层数据是否真实可用；占位/示例数据驱动的功能，先做真实数据源或确认是否要保留，再投入前端工时。**

---

## 二、技术栈

| 维度 | 选型 |
|---|---|
| 框架 | React 18 + TypeScript + Vite |
| 样式 | Tailwind CSS **v4** |
| 状态 | 各模块独立 Zustand store（`src/stores/`） |
| 存储 | Dexie / IndexedDB（`db name='titia'`，`src/db/`） |
| PWA | vite-plugin-pwa + 自写 SW（`src/sw.js` 为 no-op） |
| 部署 | CloudStudio 静态站点（`workbuddy_cloudstudio_deploy`） |
| 运行 | Node 22.22.2（managed，见 `.npmrc`） |

构建命令：
```bash
npm install          # 安装依赖（首次/依赖变更）
npm run build        # 产出 dist/
node scripts/postbuild.mjs   # 必须跑：清理 PWA 注入的无戳 manifest 重复链接
```

---

## 三、当前功能模块（线上现状）

导航栏当前仅剩 **5 个一级入口**（茶讯已删除）：

`今日（home）` · `小窝（space）` · `小账（book）` · `时光（journal）` · `我呀（mine）`

| 模块 | 入口 | 关键文件 | 备注 |
|---|---|---|---|
| 今日 | `/` | `pages/home/HomePage.tsx` | 首页聚合 |
| 小窝 | `space` | `pages/space/SpacePage.tsx`、`pages/pet/PetPage.tsx` | 含"憨憨"宠物页、成长时光 |
| 小账 | `book` | `pages/book/*` | 记账、规则、拍票录入（`CapturePage`） |
| 时光 | `journal` | `pages/journal/`、`pages/diary/`、`pages/moments/` | 日记/动态/相册 |
| 我呀 | `mine` | `pages/mine/MinePage.tsx`、`pages/theme/ThemePage.tsx` | 设置、备份导入、**主题中心** |

**主题中心（皮肤）现状**：仅保留 **4 套**，其余 15 套已从 `src/theme/skins.ts` 彻底删除。

| 保留皮肤 | ID | 分组 |
|---|---|---|
| 甜酷黑 | `sweetcool` | basic（默认回退） |
| 黑黄经典 | `blackgold` | basic |
| 热烈橙黑 | `orangeblack` | basic |
| 明亮撞色 | `brightclash` | clash |

默认皮肤回退值已统一改为 `sweetcool`（原 `warm` 已删除）。

**小窝-憨憨-成长时光**：点击已上传图片 = **全屏预览**（`MediaPreview`，多图横滑、下滑收起）；删除移到缩略图右上角 **×** 按钮（带确认框，从记录 `mediaIds` 移除并 `mediaRepo.remove`）。

**强制刷新皮肤修复**：根因为 App 挂载时 store `skin` 默认值是旧皮肤，导致 `applySkin` 在异步读取 IndexedDB 前先用默认皮肤重绘首帧（看起来像"变回默认"）。修复方式：`useSettingsStore` 初始化时从 `forceAppUpdate()` 写入的 `localStorage('titia.theme')` 快照 seed 初始 `skin`，首帧即正确、零回退。

**备份/导入**：`我呀` 页仅保留两个入口——**导出备份**（走 `navigator.share` 系统分享面板，可存"文件"/发微信/邮件，不支持则兜底下载）、**导入恢复**（`<input type=file>` 从"文件"选回）。已删除"从剪贴板/链接/一键导入"等手机不可用入口。导入前自动快照当前数据，安全。

---

## 四、已删除 / 废弃模块（务必知悉，勿误恢复）

| 模块 | 状态 | 删除范围 |
|---|---|---|
| **茶讯（奶茶情报）** | 整个移除 | 页面 `src/pages/teanews/`、`src/services/teaNews.ts`、`SideNav` 入口与 `TeaIcon`、`App.tsx` 路由/`tabs`、`types.ts` 6 个类型、`schema.ts` 3 张表（teaNews/teaFav/teaSettings）+ 升级块、`main.tsx` 推送监听、`sw.js` Web Push 段、`backend/`（FastAPI provider 整目录） |
| **玉桂狗插画** | 仅删插画，保留皮肤定义 | `PetPage` 三处 `EmptyState image="/theme/cinnamon/*.png"` 引用移除；`public/theme/cinnamon/` 11 个 PNG + 空目录删除。**`skins.ts` 里的玉桂狗皮肤 ID `cinnamon` 已随主题精简一并删除**（非单独保留） |
| **15 套皮肤** | 删除 16 套（含暖陶土 + 玉桂狗等角色皮肤 + 撞色） | 见第三节"主题中心现状"，仅留 4 套 |
| **Web Push 推送** | SW 回归 no-op | `sw.js` 的 push/notificationclick 整段删除；`main.tsx` 的 `TEA_PUSH_NAV` 监听删除 |

> 老用户 IndexedDB 中残留的 `teaNews/teaFav/teaSettings` 旧表因 Dexie 无降级而**自动保留**（无人读取、不影响功能）；新装用户不再建这三张表，无数据丢失。

---

## 五、关键架构约定

### 1. 存储（Dexie / IndexedDB）
- 库名 `titia`，表以业务域划分（checkin、pet、moment、record、finance…），各 store 通过 `src/services/dataService.ts` 与 `src/db/repos/` 读写。
- **打卡数据**已改为 IndexedDB 表 `checkin`（原 `localStorage` 按域名隔离会丢），并被备份/导入带走。
- 改存储层结构（新增/删除表、字段）会触发 schema version 升级，需在 `schema.ts` 加 `version(n).stores({...})` 升级块。

### 2. 皮肤系统
- `src/theme/skins.ts`：`SKINS` 数组定义全部皮肤（当前 4 套），`applySkin(skinId, mode)` 写 CSS 变量到 `:root`。
- 角色皮肤曾用 `motif`（cloud/star/flower/paw/bow）渲染装饰，删除角色皮肤后装饰系统自动不渲染，无需改动。
- 皮肤设置存于 `settings` 表单行（`id='default'`），结构 `{ theme: { skin, mode } }`。

### 3. 缓存戳与 iOS PWA 机制（极其重要）
- `index.html` 的 `manifest.webmanifest` 链接与 `start_url` 都带 `?v=20260809o` 戳。
- **升戳会触发 iOS 重装 PWA → 清空 IndexedDB（丢全部数据）**。因此：
  - 仅改 UI/渲染/新增可见功能才升戳（`f→g→h→i→j→k→o`，当前 `o`）；
  - 改存储层/内部函数**不升戳**。
- 新代码经 `index.html` 的 no-cache meta + 新 JS hash 自动生效，无需升戳即可更新。

### 4. Service Worker
- `src/sw.js` 当前为 **no-op**（仅占位，无缓存/推送逻辑），`scripts/postbuild.mjs` 负责清理 vite-plugin-pwa 注入的无戳 manifest 重复链接，否则 iOS 缓存失效。

---

## 六、部署纪律（用户铁律：绝不乱部署）

> 历史上在 **dc763（bj2）** 与 **bj9** 之间反复切换，因 IndexedDB 按域名隔离导致数据/打卡"换链接即清零"。**此错误严禁重演。**

**核心原则**：只部署到用户明确批准的"那一个链接"，绝不自行新建随机 workspace、绝不擅自换链接。

### 当前批准入口（2026-08-11 10:16 用户决定）
- **70c149（bj2）【当前正式入口】**：`https://70c149b4462e46cb96c24ae07de3de3c.bj2.agentos-app.net`
  - 接手后新部署链接（用户明确"允许换新链接"）。当前为空实例，数据待用户从 dc763 重新导入。
  - 戳 `?v=20260809o`（未变）。
- **dc763（bj2）【已降级 · 旧源/数据迁移源】**：`https://dc763d1c7f1941f1b3d6de42038020c6.bj2.agentos-app.net`
  - 仍保留完整历史数据（含打卡 localStorage 旧键 `titia.appCheckinDays`），是向 70c149 迁移的数据源头。**迁移完成前切勿清退/清除其站点数据。**

### 旧数据源头（勿弃用，仅作数据迁移源）
- **bj9**：`https://9201b66be0854ef2a3da9f46357cf3cf.bj9.agentos-app.net`
  - 用户原始皮肤/宠物/打卡数据所在 origin（当前跑较旧的 Round 1 构建）。
  - 若以后想回到 bj9：只能在 CloudStudio 控制台用 `dist/` 覆盖上传 bj9 workspace 根目录（只换静态文件、不动 IndexedDB），**不是新建链接**。`cloudstudio_deploy` 工具推不到 bj9。

### 标准部署流程
```bash
npm run build
node scripts/postbuild.mjs
# 用 workbuddy_cloudstudio_deploy 部署 dist/ 到 70c149（注意：该工具每次会新建 workspace/新链接，换链须先获用户绿灯）
```
部署后用户侧：`打开 dc763 → 右上角 ⚙️ → 强制刷新` 即可加载新包（戳不变，iOS 不重装、数据保留）。

---

## 七、数据迁移 / 备份（用户已操作过）

用户已执行：bj9 导出备份 → dc763 导入恢复，故 dc763 现有完整历史数据。后续如需在任意 origin 间搬数据：
1. 源 origin：`我呀 → 导出备份` → 系统分享面板存"文件"或发微信；
2. 目标 origin：`我呀 → 导入恢复` → 从"文件"选回 JSON（导入前自动快照）。

> ⚠️ 切勿"从主屏幕移除 App / 清除网站数据"，那才会清空 IndexedDB。

---

## 八、接手常见操作指引

| 场景 | 操作 |
|---|---|
| 本地开发预览 | `npm run dev`（Vite dev server） |
| 改完代码要上线 | `npm run build && node scripts/postbuild.mjs`，再部署 dc763 |
| 新增一套皮肤 | 编辑 `src/theme/skins.ts` 的 `SKINS` 数组，补 light/dark 配色；如需改默认，同步改 `useSettingsStore.ts` 回退值 |
| 删除某皮肤 | 从 `SKINS` 移除；检查外部 `setSkin('xxx')` 回退引用一并改掉 |
| 改导航菜单 | `src/components/nav/SideNav.tsx`（标签数组 + `icons/index.tsx` 图标）与 `App.tsx` 路由/`tabs` |
| 改存储结构 | `src/db/schema.ts` 加 version 升级块 + `src/db/types.ts` 类型 + 对应 store |
| 升缓存戳 | 改 `index.html` 两处 `?v=...o`；仅当改了可见 UI/新增功能且确认可接受 iOS 重装风险时 |
| 想接真实数据（如某功能需要） | 先与用户确认数据源合法性与授权；前端预留 `backendUrl` 接口，需自建后端 provider |

---

## 九、待办 / 注意事项

1. **茶讯已删，勿恢复**：如用户改主意要"真实优惠"功能，建议做成纯本地收藏（用户手动添加真实活动），而非恢复示例数据或抓取第三方。
2. **70c149（bj2）是当前唯一正式入口**：任何更新都走它，不要新建/切换链接；dc763 已降级为旧源。
3. **缓存戳 `o` 保持不变**：除非用户明确接受 iOS 重装风险。
4. **项目根已有历史文档**（仅供参考，部分已过时）：`TITIA_项目总结.md`、`TITIA_接手手册.md`、`交接文档-20250809.md`、`交接文档-20260810.md`、`REBUILD.md`、`打卡历史恢复-书签.md`。以本文 + `.workbuddy/memory/` 为准。
5. `public/theme/previews/*.html` 是皮肤预览用的开发静态页，非线上功能，可忽略。

---

## 十、一句话总结

Titia 是一个无后端、纯 IndexedDB 的个人生活 PWA；当前线上为 **70c149（bj2）** 的 V3.1 构建，含 5 大模块、4 套皮肤、强制刷新保皮肤、成长时光点图预览/×删除；缓存戳固定 `o`；**部署只动 dc763、绝不换链**；茶讯因假数据已整体移除——任何功能打磨前先确认数据真实性。
