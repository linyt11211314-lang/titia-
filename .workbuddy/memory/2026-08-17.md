# 2026-08-17

## 移除 Titia 打卡 9 天一次性种子代码（自动化定时任务）
- 文件：`src/services/checkin.ts`
  - `ensureCheckinMigrated()` 内删除了「checkin 表为空则 seedBaselineStreak(9)」的 if 块 + 上方注释，保留 `migrateLegacyCheckin()` 与 try/finally。
  - 删除了 `seedBaselineStreak(n)` 函数定义。
- 全仓无 `seedBaselineStreak` 残留引用；`src/main.tsx:91` 仍调用 `ensureCheckinMigrated()`（保留）。
- `npm run build` 通过（Exit Code 0，无 TS 错误）。
- 未部署（严守纪律，未改入口链接）。
- 备注：`src/backup.ts:200` 仍有提及该种子的旧注释，未改动（按用户要求只做移除+构建）。
