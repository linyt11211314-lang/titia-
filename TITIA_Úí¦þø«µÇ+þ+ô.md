# Titia 时序 · 项目总结

> 本总结基于接手手册、技术方案 V1.1、开发方案 V1.0、开发计划 V1.2 定稿及历次迭代开发记录整理,反映项目当前真实状态。
> 生成日期:2026-08-06

---

## 1. 产品一句话

**Titia 时序** 是一款个人「生活时间轴」记录 App,以 PWA 形式添加到 iPhone 主屏后全屏运行。它把日记、宠物成长、我们的时光、灵光一闪、待办、购物、小账、倒数日、密码箱等分散的生活片段,统一收进一条按时间排序的时间轴。基准设备 **iPhone 14 Pro(393×852 pt)**。

定位三方向(对应强调色):

- **过去 / 有温度的记忆** → 陶土橙(日记、我们的时光、人生事件)
- **现在 / 未来 / 要处理的事** → 墨绿(待办、购物、财富/小账、密码箱)
- **生活里的光 / 轻盈** → 琥珀黄(我的憨憨、灵光一闪、生理周期)

---

## 2. 技术栈(React 版定稿)

| 用途 | 选型 | 说明 |
|------|------|------|
| 框架 | react + react-dom 18 | |
| 构建 | vite 6 | dev 态秒起 |
| 语言 | typescript 5 | 数据模型靠类型兜底 |
| 样式 | **tailwindcss v4** | CSS-first 配置;**不引入任何 UI 框架** |
| 路由 | react-router 7(**hash 模式**) | PWA 任意静态托管免 rewrite |
| 状态 | **zustand** | 选择器精确订阅,store 可在组件外调用 |
| 存储 | **dexie**(IndexedDB) | 索引、版本迁移 |
| 日期 | dayjs | |
| PWA | vite-plugin-pwa | 构建期插件 |
| Excel | xlsx | 咔皮记账导入/导出 |

**刻意不引入**:MUI/Ant/Chakra、Redux Toolkit、framer-motion、react-query、第三方图标库、uuid/nanoid(用 `crypto.randomUUID()`)。总运行时依赖约 95KB gzip。

### 2.1 分层架构

```
视图层  pages / components          只渲染 + 派发事件(禁止直接读写存储)
状态层  zustand stores(按业务域)    内存镜像 + 派生计算 + UI 态
数据层  DataService(services/dataService.ts)  统一数据访问层(页面/Store 唯一入口)
领域层  services                    timeline / backup / capture / vault / syncService(接口预留)
仓储层  repositories(统一 CRUD 契约) 唯一数据出入口(铁律:组件不碰 db.*)
存储层  Dexie(IndexedDB)            可替换点(未来换远端 API)
同步层  SyncService(接口预留)        未来:DataService → SyncService → 云端数据库(当前不接云)
```

### 2.2 三层导航

Level 0 Tab 根页(首页/记录/空间/我的,常驻保活)→ Level 1 模块页(push 右滑入)→ Level 2 弹层(Sheet/Modal,支持 iOS 边缘右滑关闭)。

自建关键点:**Tab 保活**(懒挂载 + 常驻显隐 `display:none` + `inert`)、**PageStack 转场**(约 80 行自研页面栈,transform/opacity,320ms cubic-bezier)。

---

## 3. 已上线模块清单

| 模块 | 页面/文件 | 状态 |
|------|-----------|------|
| 首页时间轴 | `HomePage.tsx` | ✅ |
| 记录 | `RecordPage.tsx` | ✅ |
| 空间 | `SpacePage.tsx` | ✅ |
| 我的 | `MinePage.tsx` | ✅(Banner 固定标题 + 云同步入口 + 设置) |
| 日记 | `DiaryPage.tsx` | ✅ |
| 我们的时光 | `MomentsPage.tsx` | ✅ |
| 灵光一闪 | `SparkPage.tsx` | ✅ |
| 我的憨憨 | `PetPage.tsx` | ✅ |
| 待办 | `TodoPage.tsx` | ✅ |
| 购物 | `ShoppingPage.tsx` | ✅ |
| 倒数日 | `CountdownPage.tsx` | ✅(多轮迭代,含农历/周期/顶部总结) |
| 生理周期 | `CyclePage.tsx` | ✅ |
| 主题中心 | `ThemePage.tsx` | ✅(多皮肤,`skins.ts` + `SkinBackdrop`) |
| 小账(记账) | `BookPage.tsx`(1988 行) | ✅ 核心模块,持续迭代 |
| 一键拾光(自动记账) | `CapturePage.tsx` | ✅ 剪贴板通道解析 |
| 密码箱 | `VaultPage.tsx` | ✅ PBKDF2 + AES-GCM |
| 天气 | `services/weather.ts` | ✅ |
| AI | `services/ai.ts` | ✅ |

---

## 4. 小账模块现状(重点)

### 4.1 记账表单(`BookPage.tsx`)

- 记账表单支持收支切换、金额、商户、分类、账目(账户)、日期、备注、图片附件。
- 分类选择:**内联横向自适应网格**(`flex-wrap` + `min-w-80px` 按钮),选中蓝色高亮;一级分类 + 移动式二级展开。
- 图片附件模块(压缩上传,`media` 表存 Blob)。
- 导入/导出:支持 **咔皮记账 xlsx 格式**(双 sheet:收支账单 13 列 / 内部转账 7 列),`exportKapi` / `importBook` 均内置。
- 记账默认账户:当前版本已支持手动选择账户。

### 4.2 一键拾光(`CapturePage.tsx` + `services/captureParser.ts`)

- **剪贴板通道**:iOS 快捷方式以 `TITIA_CAPTURE::` 前缀向剪贴板写数据 → PWA 手动点击 📥 按钮读取。
- **唯一入口**:因 iOS WebKit 剪贴板读取必须用户手势,已**彻底移除全局自动读取**,仅保留 📥 按钮作为唯一入口。
- **OCR 解析器**(`captureParser.ts`):关键词附近金额提取(交易详情/实付/金额)、商户过滤(支付平台噪音/括号/OCR 噪声词)、基于商户的规则匹配、账户映射(零钱→微信)、时间提取、信心置信度评分。
- **确认弹窗流程**:先预览(账单确认标题、金额/类型/商户/分类/账目/日期/备注),字段可点击编辑 → 保存写入 IndexedDB → 取消不保存。防重复提交。

### 4.3 账务体系

- 账户体系:`useAccountStore.ts`(账户 CRUD)。
- 财务:`useFinanceStore.ts`、预算 `useBudgetStore.ts`、分类 `useCategoryStore.ts`。
- 记账:`useBookStore.ts`(默认规则、学习闭环、`applyBalance`、迁移到咔皮记账格式)。

---

## 5. 密码箱(Vault)现状

- **加密方案**:PBKDF2 派生密钥 + AES-GCM,主密码从不存储在本地。
- **已修复的解锁 bug**:根因是 `loadItems` 遇到损坏条目抛异常,把解锁阻塞住 → 改为**逐项 try/catch + damagedCount 持久提示**,损坏条目不阻止解锁,提示用户数据损坏数量。

---

## 6. 数据与同步现状

- **存储**:全部数据在本地 IndexedDB(Dexie 表:`records` `pets` `petHealth` `media` `todos` `people` `settings` `shopping` `financeItems` `cycles` `vaultMeta` `vaultItems` 等)。
- **统一数据层**:小账数据(账单/规则/账户/分类/预算/设置)统一经 **DataService**(`services/dataService.ts`);页面组件禁止直接读写存储。
- **同步预留**:`services/syncService.ts` 提供 SyncBackend 接口/变更队列注册能力,当前不接任何云;`services/webdav.ts`(旧 Supabase 实现)保留未删。
- **跨容器桥**:Safari ↔ PWA 通过 localStorage(`titia.pendingTx`)经 DataService 统一读写;App 启动先合并桥再水合 store,保证两入口数据一致。
- **当前阶段决策(最新需求文档)**:**本地存储优先,不配置任何 API、不接入 Supabase/后端/云端接口**,未来需要多设备同步时再通过 SyncService 单独接入。

---

## 7. 最新需求文档(小账模块优化 · 含开发约束)

> 文档:`Titia时序_小账模块优化需求文档_含开发约束.docx`(2026-08-06 上传)

### 7.1 开发固定约束(增量优化)

**禁止**:重构整个 App / 修改已有数据结构 / 修改无关模块 / 删除已有功能 / 创建重复系统。
**必须保持**:当前小账结构、账单体系、分类体系、账户体系、预算体系。

### 7.2 六项优化需求

| # | 需求 | 要点 |
|---|------|------|
| 1 | **云同步架构调整** | 仅本地存储;不接 Supabase/后端/云端;不配置 API;未来再单独加云同步 |
| 2 | **资产页面优化** | 顶部资产总览卡片**固定**(净资产/资产金额/负债金额);下方账户列表+入口**可滚动**;禁止左右滑动 |
| 3 | **资产账户详情页** | 点账户进独立详情页;顶部月度总览卡片固定(本月收入/本月支出/当前余额);下方账单明细可滚动,按账户筛选收支 |
| 4 | **分析页面优化** | ① 月份筛选(选月看当月数据);② 柱状图联动(点月份柱→当月消费详情);③ 分类占比联动(点分类→对应分类账单明细) |
| 5 | **记账默认账户** | 新增支出账单默认账户 = **招商银行信用卡**,用户可手动改 |
| 6 | **自动识别按钮优化** | 点击后直接执行 读取→解析→生成结果,不出现「粘贴/剪贴板文字/中间操作提示」字样;保持自动读取+OCR+分类匹配+生成预览+保存账单 |

### 7.3 测试要求

- 云同步:确认本地数据正常保存。
- 资产:顶部卡片固定、明细滚动、账户详情正常。
- 分析:月份筛选、柱状图跳转、分类明细查看正常。
- 记账:默认账户为招商银行信用卡且可修改。
- 自动识别:点击后直接识别、不出现粘贴提示。

---

## 8. 测试体系

### 8.1 E2E 脚本(`scripts/e2e/`)

- `run-all.sh` 统一执行全部回归(共 19 组):`bookcheck` / `bookcheck3` / `bookcheck4`(小账回归,断言「导出账单 Excel」)、`capturecheck`(一键拾光流程,断言账单确认标题+商户+日期)、`scrollfix` / `scrollcheck2`(滚动修复)、`tabcheck` / `navcheck` / `clashcheck` / `mixcheck` / `interact3`(导航交互)、`countdown*`(倒数日)、`settingscheck` / `skincheck` / `ptrcheck` / `aicheck` / `supabasetest` 等。
- 早期全量回归基线:**156/156,0 控制台报错**(阶段四 24/24 + 39/39 + 13/13、阶段五 96/96、phase6 18/18、phase7 42/42)。
- 运行方式:`npm run e2e`(本地)或 `npm run e2e:live`(线上 `https://a149a628a3c099573.sh7.agentos-app.net`)。

### 8.2 历史 Bug 修复记录

- **Banner 高度为 0**:flex 容器压缩 inline height → `shrink-0` 修复。
- **密码箱解锁失败**:损坏条目抛异常阻塞解锁 → 逐项 try/catch + damagedCount。
- **iOS 粘贴提示干扰**:全局剪贴板自动读取 → 移除所有全局触发,保留 📥 唯一入口。
- **咔皮导出**:需 xlsx;导入时 Excel 空行导致 `sheet_to_json` 失败、sheet 缺失报错 → 已处理。
- **vite preview 启动竞争条件**、测试遗留 `/tmp/cdp-*` 清理问题。

---

## 9. 部署状态

- 线上地址:`https://a149a628a3c099573.sh7.agentos-app.net`(PWA,含 `sw.js`、`manifest.webmanifest`、全套图标)。
- `dist/` 已构建,含 `theme/`(多主题产物)。
- 另有 `cloudflare-worker/` 目录(云函数备用)。
- 备份文件:`titia-backup-2026-08-05.json`(3.6MB,数据导出)。

---

## 10. 待办事项(按优先级)

### 10.1 小账模块优化(最新需求文档)

> ✅ **已于 2026-08-06 全部实施完成**并部署上线(`https://a149a628a3c099573.sh7.agentos-app.net`),本地/线上 e2e 全量回归通过。

1. ✅ **云同步架构调整**:MinePage 移除「云同步」配置入口;App 启动、下拉刷新(reload.ts)、一键拾光保存后均不再调用 Supabase;`webdav.ts` 服务文件保留(未来多设备同步时再接入)。线上产物已无「云同步」字样。
2. ✅ **资产页**:顶部资产总览卡片(sticky 固定,净资产/资产金额/负债金额)+ 下方账户列表可滚动;无横向可滚动容器(禁止左右滑动)。
3. ✅ **资产账户详情页**:点击账户进入独立详情页;顶部月度总览卡片固定(本月收入/本月支出/当前余额),下方按账户筛选的支出+收入明细可滚动;编辑/删除账户入口保留在顶部;余额编辑后详情页实时刷新。
4. ✅ **分析页**:月份左右切换筛选(数据/三卡/分类占比/月度报告/明细全联动);柱状图点击月份→切换选中月并显示当月消费详情;分类占比行点击→对应分类账单明细(再点清除筛选)。
5. ✅ **记账默认账户**:新增支出账单默认账户 = 招商银行信用卡(按名称精确匹配,退而匹配 type=信用卡且银行名含「招商银行」),用户可手动修改;编辑模式保留原账户。
6. ✅ **自动识别按钮**:点击 📥 后直接 读取→解析→生成识别结果,无「已读取,正在识别…」等中间操作提示;OCR 原文默认折叠。注:iOS WebKit 系统级「允许粘贴」弹窗由平台弹出,前端无法屏蔽(已注释说明)。
7. ✅ **回归测试**:本地 19 组全过(含新增断言:sticky 固定/详情页/月份筛选/柱状图联动/分类联动/默认账户可修改/禁止横滑/无中间提示);线上 18 组全过(aicheck 因 HTTPS mixed-content 限制仅本地跑,本地 8/8 通过)。bookcheck 本地与线上均 42/42。

### 10.2 Safari 与 PWA 数据同步架构优化(2026-08-06 新增)

> ✅ **已实施完成**并部署上线。Local First + Sync Ready 架构,当前不接任何云 API。

1. ✅ **DataService 统一数据访问层**(`src/services/dataService.ts`):页面/Store → DataService → IndexedDB(Repository) → SyncService → 云端(未来)。封装 bills(transactions)/rules/accounts/categories/budgets/settings 的 `saveBill/getBills/updateBill/removeBill`、`saveAccount/updateAccount`、`saveBudget/updateBudget` 等业务 API;仅转发 Repository,**不改变字段含义/业务逻辑/数据结构**,已有 IndexedDB 数据无需迁移、原样兼容。
2. ✅ **SyncService 接口预留**(`src/services/syncService.ts`):定义 `SyncBackend` 契约(push/pull)、`SyncChange` 变更单元、`registerBackend` 注册能力;写操作经 DataService 统一 `markDirty` 记录变更(行级 `_dirty` 由 Repository 双保险维护);`syncNow()` 当前恒返回 `no-backend`,不产生任何网络请求。
3. ✅ **页面组件禁止直接读写存储**:BookPage 的 `titia.editTxId`、CapturePage 的 `titia.pendingTx` 等 localStorage 直读已全部收拢到 DataService(跨容器桥 `queuePendingBill/takePendingBill/mergePendingBills/markEditBill/takeEditBillId`);小账 store(useBookStore/useAccountStore/useCategoryStore/useBudgetStore/useSettingsStore)全部改走 DataService。
4. ✅ **跨容器同步链路加固**:修复「Safari 写入桥 → PWA 打开合并」与 store 加载的竞态——App 启动先合并桥再水合 store,合并后刷新小账内存,保证本次会话立即可见。
5. ✅ **专项 e2e `synccheck.mjs`**(本地/线上 10/10):Safari 写入→PWA 打开自动合并入库(字段含义不变)、PWA 新增保存、重新打开数据不丢失、历史数据正常显示编辑。
6. ✅ **回归**:本地 20 组全过(19 + synccheck);线上 19 组全过(aicheck 仅本地)。

### 10.3 Safari 与 PWA 数据同步 UI 刷新机制(2026-08-06 追加)

> ✅ **已实施完成**并部署上线。修复「数据层已同步但 PWA 页面状态不刷新」问题,仅新增刷新机制,未改数据体系。

1. ✅ **数据变化通知(billChanged)**:DataService 的 `saveBill/updateBill/removeBill` 及跨容器合并 `mergePendingBills` 成功后,统一触发 `titia:billChanged` 事件(新增 `BILL_CHANGED_EVENT` 常量,不改动现有 API)。
2. ✅ **页面监听自动刷新**:`useBookStore` 模块级监听 billChanged,收到通知后重新 `getBills()` 刷新列表;页面经 store 订阅自动重渲染,无需手动刷新。
3. ✅ **PWA 前后台切换自动重读**:`watchCrossContainerSync()`(reload.ts)监听 `storage`(跨容器桥 `titia.pendingTx` 变化)/ `visibilitychange`(回前台)/ `focus`,统一触发 `reloadAll()` 自动重读 IndexedDB(300ms 防抖);App.tsx 启动时挂载。
4. ✅ **普通记账写桥补齐**:BookPage 新增账单保存后写入跨容器桥 `queuePendingBill(tx)`(此前仅一键拾光写桥),保证 Safari/PWA 双向普通记账都能触发对端自动刷新。
5. ✅ **专项 e2e `crosscheck.mjs`**(双页面同源容器,本地/线上 10/10):① Safari 新增→PWA storage 事件无需刷新自动出现;② PWA 前台切换(focus/visibilitychange)自动重读;③ 反向 PWA 新增→Safari 自动出现、两容器数据一致。
6. ✅ **回归**:本地 21 组全过;线上 20 组全过(aicheck 仅本地)。

### 10.4 小账账单管理能力优化(2026-08-07)

> ✅ **已实施完成**并部署上线。仅优化账单核心逻辑(多笔识别/金额优先级/重复检测/批量删除/删除回滚/附件清理),未改数据体系与统计逻辑。

1. ✅ **多笔支付订单支持(需求三)**:`captureParser` 新增 `parseMultiCaptureText` 逐行拆分「商户 金额」交易单元(过滤订单号/状态/平台噪声行,允许美团外卖/滴滴等消费平台);`CapturePage` 新增多笔确认 UI——「识别到 N 笔消费」+ 每笔候选勾选(默认全选,分类联动)+「全部保存(N)」批量入库;不足 2 笔自动回退单笔流程(兼容现有)。
2. ✅ **金额识别优化(需求四)**:`parseAmountFromText` 按优先级匹配 ①支付金额/实付 → ②实际交易金额/金额/合计 → ③交易详情 → ④货币符号 → ⑤xx元 → ⑥负号 → ⑦兜底;先剔除优惠/立减/折扣/满减金额;支持 `-10.60 / ¥10.60 / ￥10.60 / 10.60元`。
3. ✅ **重复记账检测(需求五)**:`useBookStore.findDuplicate` 按 金额+交易对象+时间(≤2分钟)+账户 组合比对;保存前(单笔与多笔批量)命中即弹「可能重复记账」Sheet,提供「继续保存/取消」;取消不落库。
4. ✅ **批量删除(需求六)**:账单页新增「编辑」管理模式——多选(全选/点选)+「删除选中(n)」+二次确认;管理模式隐藏单笔删除按钮。
5. ✅ **删除余额回滚(需求七)**:`useBookStore.remove` 删除后 `await applyBalance(old, -1)` 回滚——删除支出恢复账户金额、删除收入扣除;资产/月度/预算统计均为 transactions 派生,删除后自动联动更新。
6. ✅ **附件清理(需求八)**:删除账单时同步软删关联 `mediaIds` 图片附件,避免孤立图片(一键拾光账单带支付截图)。
7. ✅ **专项 e2e**:`capturecheck` 扩展多笔识别+重复检测(50/50);新增 `delcheck`(14/14)覆盖单笔/批量删除、余额回滚、附件清理。本地 22 组全过;线上 21 组全过。

### 10.5 AI 识别接入 DeepSeek(2026-08-07)

> ✅ **已实施完成**并部署上线。AI 识别从 Cloudflare Worker 代理切换为 **DeepSeek 官方 API**(OpenAI 兼容接口,模型 deepseek-chat)。

1. ✅ **`services/ai.ts` 重构**:调用 `POST {base}/chat/completions`(默认 `https://api.deepseek.com`),`Authorization: Bearer <key>`,system 提示词限定输出 JSON,启用 `response_format: json_object`,15s 超时;解析 `choices[0].message.content`(兼容 ```json 代码块包裹);未配置/失败静默返回 null,调用方降级本地规则。
2. ✅ **配置入口(我呀页)**:AI 设置表单改为「DeepSeek API Key」(必填)+「API 地址(可选,默认 DeepSeek)」+ 保存/清除;卡片标题「AI 识别(DeepSeek)」,按 Key 显示已启用/未配置。Key 仅存 localStorage(用户自用 PWA,不写入构建产物)。
3. ✅ **e2e 适配**:`aicheck.mjs` mock 服务器改为 DeepSeek `/chat/completions` 响应格式,配置改为 `titia.aiKey` + `titia.aiBaseUrl`;本地 8/8 通过(线上 https 因 mixed-content 仅本地跑,与既有约定一致)。
4. ✅ **回归**:本地 22 组全过;线上 21 组全过。
5. ⚠️ **使用方式**:我呀页 →「AI 识别(DeepSeek)」→ 填入 DeepSeek API Key(sk-…)→ 保存即可;记账表单「🤖 AI 识别」按钮与一键拾光 AI 兜底自动生效。
6. ✅ **输入框修复(2026-08-07)**:修复「AI 设置表单无法输入」——根因是 Sheet 内容经 `useOverlayStore.open()` 存储为 ReactNode **快照**,内联输入框绑定父组件 state 时受控 value 被快照锁定,输入即回滚;已改为独立组件 `AiConfigForm`(自管 useState,与其他 Sheet 表单一致),并固化 e2e 断言「API Key 可输入(受控回滚修复)」;aicheck 本地 12/12,本地 22 组 / 线上 21 组全过。

### 10.6 自动记账智能识别规则更新(2026-08-07 · 用户指定规则)

> ✅ **已写入并部署上线**。用户明确指定的智能识别规则,优先级:用户指定 > 本指令规则 > 系统默认规则。

1. ✅ **统一规则配置 `src/config/aiRules.ts`**:12 类分类体系(餐饮/交通/购物/居住/娱乐/通讯/医疗/学习/人情/转账/工资/其他)+ 每类关键词映射(单一配置源);`matchCategory` 按 商户名>摘要关键词>金额辅助 匹配,兜底「其他」;`normalizeMerchant` 去公司后缀/括号分店/平台后缀(瑞幸咖啡(深圳)有限公司→瑞幸咖啡);金额阈值(大额 1 万/小额 0.01/公式容差 0.01)、去重窗口 5 分钟、合并窗口 2 分钟/2 小时、周期性 3 个月±10% 常量。
2. ✅ **AI 提示词升级(`ai.ts`)**:DeepSeek 输出扩展字段 `{merchant, category(限定12类), account, amount(元,支出负/收入正), date, memo, orderId, warn, periodic}`;提示词内置金额实付优先、合并规则(同订单/同商户≤2分钟合并为一条)、去重规则(商户+金额±0.01+时间≤5分钟)、校验规则(公式/大额/小额/三位小数);金额输出转分与存储对齐。
3. ✅ **规则引擎 `src/services/billRules.ts`**:`validateAmount`(正负号自动修正/大额/小额/三位小数)、`checkFormula`(原价-优惠-抵扣=实付)、`mergeSubTransactions`(同订单号/同商户≤2分钟合并,金额求和、取最早时间、分类取金额最大、备注【合并】)、`dedupeBatch`(商户+金额±0.01+时间≤5分钟判重,退款对冲豁免)、`isPeriodic`(连续 3 月相近金额)、`summarize`(总笔数/支出/收入/去重/合并/复核汇总)。
4. ✅ **分类规则库替换(`useBookStore` DEFAULT_RULES)**:19 条旧规则(咖啡/午餐/外卖/打车等二级)替换为用户 12 类精选关键词(瑞幸→餐饮、滴滴→交通、淘宝→购物、信用卡还款→转账、工资→工资 等);`findDuplicate` 扩展:时间窗 2→5 分钟、金额容差 ±0.01、支持订单号/流水号维度、退款对冲不判重。
5. ✅ **多笔拆分与合并不冲突**:不同商户截图 → 拆分保存(便捷录入);同一订单子交易 → 合并为一条(避免重复)。本地拆分路径保持,AI 输出层按合并规则处理。
6. ✅ **验证**:`rulescheck.mjs`(13/13)覆盖商户标准化/新分类/拆分/5 分钟去重;`billRules` 纯函数单元 17/17(校验/合并/去重/周期性/汇总);受影响测试断言同步更新(瑞幸/星巴克/海底捞 分类咖啡→餐饮);本地 23 组 / 线上 22 组全过。
7. ⚠️ **说明**:新分类只影响**新识别**的账单,已存历史账单分类保持不变(不破坏数据);分类面板仍为原 12+ 一级分类体系,识别规则与其并行(识别归入「餐饮」等一级类,面板内二级细分不受影响)。

### 10.7 平台专属解析规则(2026-08-07 · 拼多多订单页优先匹配)

> ✅ **已写入并部署上线**。新增「平台专属解析规则」模块,优先于通用金额提取。

1. ✅ **`src/services/platformRules.ts`(新增)**:判定拼多多订单页(含「拼团中/7天无理由退货/免运费」关键词);金额提取按优先级 `实付：¥x` → `实付 ¥x` → `¥xx.xx`;提取金额必须 ≤ 该商品区块原价(`·39.9`),否则丢弃重匹配;同一区块多金额取最小值;以「7天无理由退货/更多」分隔商品区块;提取不到标记「金额识别失败」,不猜测不补全。
2. ✅ **接入 `captureParser`**:`parseAmountFromText` 检测到拼多多特征时优先走平台规则,平台页未识别则直接失败(不降级通用规则,避免误取原价/件数)。
3. ✅ **测试用例验证(8/8)**:用例1 `实付：¥3.77` → 3.77 ✓;用例2 `实付：¥15.8` → 15.8 ✓;用例3 `·39.9 x1 实付：¥32.9` → 32.9(忽略原价)✓;附加:无冒号 `实付 ¥5.5`、兜底 `¥12.34`、提取失败返回 null、非拼多多走通用规则 ✓。
4. ✅ **回归**:本地 23 组 / 线上 22 组全过;修复 navcheck 偶发失败(残留 chromium 占用端口导致连到旧页面,已加固「先离开再回来」)。

### 10.8 待办与购物清单「点击即消失」(2026-08-07)

> ✅ **已实施完成**并部署上线。点击完成/已买后立即从列表消失,无需手动删除;数据不删(仅 UI 隐藏,折叠区可回顾/恢复)。

1. ✅ **待办(TodoPage)**:主列表只显示**未完成**项(点击完成 → 立即消失);已完成项收进底部「已完成 N 项」可展开折叠区(默认收起,可回顾/取消完成/删除);全部完成时显示「全部完成 🎉」。
2. ✅ **购物清单(ShoppingPage)**:勾选置「已买」→ 立即从「想买」列表消失;「已买 N 项」折叠区默认收起,可展开回顾/恢复/移除。
3. ✅ **首页联动(HomePage)**:待办速览只显示未完成项(点击完成即消失);购物速览本就只显示待买(不变)。
4. ✅ **约束遵守**:仅改 3 个页面 UI 展示逻辑;未动 store/数据结构/其他模块数据;已买/已完成数据仍保留在 IndexedDB。
5. ✅ **回归**:本地 23 组 / 线上 22 组全过(期间清理了调试残留 chromium 进程,消除端口占用导致的偶发失败)。

### 10.9 我呀页设置整合与版本号 V2.0(2026-08-07)

> ✅ **已实施完成**并部署上线。

1. ✅ **主题中心移入应用设置**:移除我呀页设置列表中的「主题中心」卡片;入口移入「应用设置」Sheet 顶部(点击 → 跳转 /theme 换肤页,皮肤与深浅说明保留)。换肤页本身未改动。
2. ✅ **版本号**:底部「Titia 时序 · Version 1.4」→「Titia 时序 · V2.0」。
3. ✅ **约束遵守**:仅改 MinePage 展示与 SettingsForm 入口;未动主题/皮肤模块、其他模块数据。
4. ✅ **回归**:适配 scrollfix 测试(移除卡片后我呀页内容变短、滚动容器不再强制产生滚动,断言改为「容器存在+配置正确」;滚动行为在内容足够时验证);本地 23 组 / 线上 22 组全过。

### 10.10 美团/微信账单多笔订单识别优化(2026-08-07)

> ✅ **已实施完成**并部署上线。修复「微信支付账单列表页(美团多笔订单)识别不成功」——商家与金额被 OCR 拆成相邻两行时无法拆分。

1. ✅ **多行聚合**:`parseMultiCaptureText` 新增「纯金额行」匹配(`·15.01`/`¥12.91`/`15.01`),向上回看最近的非噪声候选商户行(限 5 行、跨越「使用零钱支付」等噪声行)合并为「商家+金额」候选;新商户行(如「美团 美团」)入栈时清空旧栈,避免跨订单误合并。
2. ✅ **商户行字符集扩展**:支持括号/破折号/下划线(「美团） 美团」可被识别为商户)。
3. ✅ **验证(用户实际 OCR)**:「美团） 美团 … ·15.01」+「美团 美团 … ·12.91」→ 正确拆出 **2 笔 ¥15.01 / ¥12.91**;既有 capturecheck 多笔(瑞幸/美团外卖/滴滴 3 笔)、拼多多平台规则等全部保持通过。
4. ✅ **回归**:本地 23 组 / 线上 22 组全过。

### 10.11 灵光一闪联网配图(2026-08-07 · API 自动配图)

> ✅ **已实施完成**并部署上线。输入内容后保存即自动联网配图(电影海报/专辑封面/书籍封面等),无需手动点按钮。

1. ✅ **自动配图**:输入(如「电影-蜘蛛侠4」「音乐-周杰伦晴天」)→ 点「记」→ 保存后自动按内容/归类异步配图,写入 `payload.imageUrl` 展示在卡片;原手动「🔗 联网配图」按钮保留可重试。
2. ✅ **数据源(keyless、免费)**:
   - **iTunes Search API**:音乐专辑/电子书/App 官方封面(中国大陆可达、支持 CORS)——「音乐-周杰伦晴天」实测自动配到专辑封面 ✓
   - **维基百科 REST**:电影海报/词条代表图(中文电影名自动映射英文,如 蜘蛛侠→Spider-Man、流浪地球→The Wandering Earth、哪吒→Ne Zha 等 24 个常见映射)——网络可达时「电影-蜘蛛侠4」自动配海报
   - 失败优雅降级:返回 null,记录正常保存不报错(电影在维基不可达网络下无图)。
3. ✅ **媒体类型匹配**:归类电影→movie、音乐→music、研究→ebook、产品→software;无归类时按文本关键词自动判断(电影/音乐/书名)。
4. ⚠️ **已知限制**:Apple 对 iTunes 电影/电视搜索不返回数据(实测全 0)、维基百科在大陆网络常被墙——无 key 免费源下「电影海报」自动配图依赖维基百科可达性;音乐/书籍/软件配图可靠。
5. ✅ **回归**:本地 23 组 / 线上 22 组全过。

### 10.12 密码箱「无法解密」提示可手动关闭(2026-08-07)

> ✅ **已实施完成**并部署上线。修复 PWA 端密码箱顶部「有 N 条记录无法解密…」提示一直显示、无法消除的问题——改为**可手动关闭**且**按损坏数量持久化**:关一次即不再打扰,仅当损坏记录数量变化(新增损坏)时重新提醒。

1. ✅ **可关闭提示条**:`VaultPage` 提示条增加「✕ 关闭」按钮(`CloseIcon` 新增至自建图标集,纯新增导出不影响现有图标);点击后立即消失,账号列表与数据不受影响。
2. ✅ **持久化(按数量记忆)**:`useVaultStore` 新增 `damagedDismissed` 状态与 `dismissDamagedHint()` 动作;关闭时把「当时的损坏数量」写入 `localStorage['titia.vault.damagedDismissed']`。解锁/增删改后按 `shouldShowDamagedHint(failed)` 判断:同一数量不再显示(已关闭过)、数量变化时重新提醒——既尊重「手动关闭」意图,又不至于让新损坏静默。数据始终保留在库不删除。
3. ✅ **范围收敛**:仅改 `VaultPage.tsx` / `useVaultStore.ts` / 图标集新增导出;新增 localStorage 键仅影响密码箱自身提示,不触碰其他模块数据。
4. ✅ **验证**:新增 `vaultdismiss.mjs`(16 项:创建→添加→种损坏→提示出现→关闭消失→重进不打扰→数量变化重新提醒→再次关闭→清理后正常)并纳入 `run-all.sh` 常规回归。
5. ✅ **回归**:本地 24 组 / 线上 23 组全过。
6. ✅ **测试基建顺带修复**(本次暴露):① `vaultdismiss` 初始误用与 `settingscheck` 相同的调试端口 9361→改 9328;② `interact3/scrollcheck2/vaultdismiss` 原先**硬编码本地 BASE**(不读 `process.env.BASE`),导致历史「线上回归」实际测的是本地 4185——统一改为 `process.env.BASE || 本地` 后,本次线上 23 组首次**真正直连线上域名**验证通过;③ 两脚本补「等待页面导航完成」逻辑(线上加载慢时避免在 about:blank 上下文操作 IndexedDB);④ `run-all.sh` 开头增加残留 chromium 进程清理,根治「异常退出占用调试端口 → 后续脚本连错对象」的偶发失败。

### 10.13 今日打卡/日记列表/账单分组/默认账户/购物修改/全局UI(2026-08-08 · V2.1)

> ✅ **已实施完成**并部署上线(V2.1)。六大板块需求,约束「禁止修改其他功能和数据」——全部为增量能力与布局调整,账单/账户/购物/日记数据字段与既有逻辑不变。

1. ✅ **今日页打卡面板 + 消费卡片**(banner 下方、待办上方):新增 `services/checkin.ts`(独立 localStorage 键 `titia.appCheckinDays`,不影响其他模块)——「已使用天数」= 2026.8.3 起实际打开过的去重天数;「连续打卡天数」= 从今天(或最近打开日)往前连续;「今日消费 / 本月消费」直接订阅 `useBookStore.transactions` 实时计算(支出,转账与收入不计),账单变化自动刷新,数据真实同步。
2. ✅ **日记/关系列表样式 + 心情筛选 + 图片预览**:
   - 卡片式改**紧凑列表**(日期块 DD+YYYY-MM + 标题/摘要 + 心情 + ›),点击行 → **详情 Sheet**(只读完整内容 + 编辑/删除入口),不再占位置;
   - 日记顶部新增**心情筛选**(全部/😊/😐/😢/😡/😌);
   - 新增 `components/base/MediaPreview.tsx` 全屏图片预览:列表/详情中点击缩略图预览大图(阻止冒泡,不触发删除/编辑),与待办/购物清单的「点击即完成」逻辑彻底区分;关系页同套列表样式。
3. ✅ **账单默认扣款账户 + 按日分组 + 指标卡固定**:
   - 设置项「默认扣款账户」(`settings.app.defaultAccount`,应用设置内可选账户):OCR 识别内容**带账户 → 直接识别填充;未带 → 用默认扣款账户**(手工记账默认账户同样优先设置值、回退招商银行信用卡);
   - 账单明细**按日分组**:同一天归纳到一个卡片(不收起、平铺),不同日期卡片间留间隔;日期栏左侧日期+星期、**右侧当天支出/收入汇总右对齐**;
   - **顶部指标卡组(本月概览+分栏)sticky 固定**,滚动时不可上下滚动;
   - 删除回退金额:沿用既有 `remove → applyBalance(-1)`(已具备,回归覆盖)。
4. ✅ **账单双指下拉多选**:账单列表双指(两指)下滑,按「起点账单 → 当前手指位置」**范围滑选多笔**(原生非 passive touch 监听,自动进入编辑模式);顶部操作条提示「双指下拉可滑选」。
5. ✅ **购物清单可修改**:小窝/购物页每项新增「改」按钮 → Sheet 改名(`useShoppingStore.update` 新增);今日页共用同一 store,**修改后自动同步**。
6. ✅ **全局 UI 优化**:分类面板点击**无二级的一级分类(叶子)直接选中并自动收起**(有二级仍展开);新增 `services/inputVisibility.ts` 全局监听 focusin + visualViewport——唤起输入法后自动把输入框滚到键盘上方(任意行皆可,不遮挡);底部版本号 **V2.0 → V2.1**。
7. ✅ **验证**:新增 `checkincheck.mjs`(12 项)、`diarylistcheck.mjs`(12 项)、`billgroupcheck.mjs`(13 项)并入常规回归;适配 `delcheck/synccheck` 账单行选择器(卡片 `role="button"` → 原生 `button[data-bill-id]`)。本地 27 组 / 线上 26 组全过。

### 10.14 打卡重构/时光分段按月/图片预览升级/顶部不透光/羽毛笔透明度(2026-08-08 · V2.1)

> ✅ **已实施完成**并部署上线。八项需求,约束「禁止修改其他功能其他数据」——全部为增量能力与布局调整,数据字段不变。

1. ✅ **今日打卡面板重构**:
   - **已使用天数**改为「2026/8/3 至今过去多少天」(日历天数:今天-起始日+1,不再依赖打开记录);
   - **连续打卡**改为**手动打卡**:卡片内新增美观打卡按钮(主色填充「📅 打卡」),点击后连续天数 +1、按钮变「✓ 今日已打卡」并禁用,每日 0 点自动恢复可打卡(每分钟轻量检查跨日);
   - 连续打卡文案改为「真棒!今天又来看我啦～」;消费两卡(今日/本月)保持真实同步账单。
2. ✅ **时光页分段控制器 + 按月收纳**:
   - 顶部改为居中**分段控制器**(📖 日记 | 💙 关系,胶囊样式),移除左侧边栏;
   - 日记/关系列表**按月收纳**:月份卡片(「YYYY 年 M 月 · N 条」)→ 点击进入该月记录列表子视图(可返回),列表更整洁;关系页同套。
3. ✅ **图片预览升级**(`MediaPreview`):右上角关闭按钮透明度调低(不抢画面);**下滑收起**手势(跟手位移+淡化,超过阈值关闭);同一记录多张图片**左右滑动翻页**(页码提示「n / N」)。
4. ✅ **分类选择自动收起**:实测确认「点击一级展开二级 → 点击二级自动收起面板;点击无二级的一级(叶子)直接选中并收起」均正常(本轮重新验证)。
5. ✅ **全局顶部区域不透光统一**:所有页面滚动容器统一 `bg-bg` 不透明背景 + `calc(env(safe-area-inset-top)+16px)` 顶部安全区(`PageHost` 默认 + 空间/时光/小账/我呀/一键拾光自管容器)——状态栏区域不透光,滚动内容不再从顶部标题/状态栏空隙透出;标题仍显示顶部左上角;各页主单元视觉统一。
6. ✅ **羽毛笔长按调透明度**:长按 500ms → 按钮周边出现**半环绕进度条**(270° 弧,开口朝向屏幕中心、随按钮位置变化),在弧上点按/拖动调整按钮透明度(0.25~1,实时预览),松手持久化到 `localStorage['titia.fabOpacity']`;修复长按后松手被误判为点击跳转的交互缺陷(捕获释放需用 DOM 引用,合成事件 currentTarget 在异步回调中失效)。
7. ✅ **验证**:更新 `checkincheck`(打卡按钮/日历天数)、`diarylistcheck`(分段控制器/月份收纳/多图左右滑/下滑收起)、`navcheck`(分段控制器断言);新增 `fabopacitycheck`(长按→进度条→持久化→再次长按回读)。本地 28 组 / 线上 27 组全过。

### 10.15 顶部不透光修正/今日页可滚动/时光页改回左侧导航(2026-08-08 · V2.1)

> ✅ **已实施完成**并部署上线。针对用户 iPhone 14 Pro 实测反馈的三轮整改(顶部透明条、标签被遮盖、时光页导航方向理解错误)全部落实。

1. ✅ **今日页「除 banner 外可上下滚动」**:banner(问候+天气)固定不滚动,下方打卡/待办/购物清单放入**独立滚动容器**(保留下拉刷新 PullToRefresh),记录再多也不会被底部导航遮盖、可正常上下滑动。
2. ✅ **全局顶部不透光(统一兜底)**:App 壳新增**全局状态栏遮罩**(`absolute top-0` 高度 `env(safe-area-inset-top)`、背景不透明 bg-bg、pointer-events-none)——任何页面(小账/账单/资产/倒数日/今日等)滚动内容都不可能从顶部状态栏/标题空隙透出;叠加各页面滚动容器已有的 `bg-bg` + `calc(env(safe-area-inset-top)+16px)` 安全区双保险。标签(页面标题/左侧导航)仍在左上角、不被遮罩遮挡。
3. ✅ **账单页/资产页顶部**:账单页标题「小账」位于安全区下方左上角、不被指标卡遮盖;资产页顶部总览卡保持固定(sticky)但**压缩高度 + 底部阴影边界**,减少对折线统计图顶部的遮挡观感,滚动时内容从卡下方平滑经过。
4. ✅ **时光页改回左侧导航栏**(日记/关系侧边栏,撤销上一轮分段控制器——用户本意是「顶部筛选项」用分段控制器):心情筛选改为**分段控制器样式(横格排列)**,并**移入月份列表子视图内**(进入某月后顶部筛选该月日记);月份卡片列表页不再显示筛选。
5. ✅ **分类选择弹窗**:回归断言「点击一级展开二级 → 点击二级自动收起;叶子一级直接选中并收起」纳入 billgroupcheck 持续验证(代码复核无误,发布后线上同步)。
6. ✅ **验收口径**:按用户 iPhone 14 Pro(390×844 + 安全区)复核布局;e2e 更新 `ptrcheck`(今日页滚动容器下拉刷新)、`navcheck`(左侧导航)、`diarylistcheck`(月份内心情分段控制器)。本地 28 组 / 线上 27 组全过。

### 10.16 V3.0 八项整改(2026-08-08 · V3.0)

> ✅ **已实施完成**并部署上线。小账布局/分类弹窗理解修正、导入资金计算、双击灵动岛回顶、分析页固定、版本 V3.0、憨憨封面图、图片预览按钮、时光标签与关系分段,共 8 项。

1. ✅ **任务一·小账布局**:① **资产页净资产条改为 absolute 固定**(贴滚动容器视口顶部,滚动时永远固定不被推走,不再出现「净资产滚走、趋势图贴顶」的错位;下方内容以占位 div 留出空间,趋势/账户列表正常滚动);② **分类弹窗理解修正**:整张「选择分类」改为 **popover 弹出卡片**(absolute 浮层、有边框阴影),选中二级/叶子一级/完成后面板**整张合上收起**,表单只剩「分类」「账户」字段,不再占表单空间。
2. ✅ **任务二·账户资金计算**:① **导入咔皮账单不再对账户余额双倍累加**(`useBookStore.add` 新增 `silent` 参数,导入历史交易跳过 applyBalance,账户余额=导入文件当前值);② **信用卡欠款(负余额)正确计入负债总额**(liabilityTotal 取绝对值,不再被 Math.max 归零);③ 负债账户「欠款」显示为正数(原显示 -500.00 → 500.00)。
3. ✅ **任务三·双击灵动岛回顶**:全局监听顶部 0~70px(状态栏/灵动岛区域)300ms 内两次点击 → 当前页面所有可滚动容器平滑回顶(touchend + pointerup 双通道,兼容 iOS 与桌面/模拟)。
4. ✅ **任务四·分析页固定**:月份筛选 + 收支三卡 + **消费趋势 + 分类占比 + 月度报告**整体 sticky 固定,下方「当月消费详情/分类明细」独立滚动查看。
5. ✅ **任务五·版本号 V2.1 → V3.0**。
6. ✅ **任务六·憨憨封面图**:憨憨主页新增「相册封面」模块——大窗格(56 高度)预览,支持上传/替换/移除(localStorage 独立键 `titia.petCover.<petId>` 存 mediaId,零数据层迁移),适配页面空白、更饱满。
7. ✅ **任务七·图片预览按钮**:右上角关闭按钮改半透深色圆底 + 细描边,位置一眼可找,图标保持淡色不抢画面(全屏预览 + 多图左右滑动 + 下滑收起保持不变)。
8. ✅ **任务八·时光页**:顶部新增「时光」标题标签(左上角,安全区避让);关系页「感动瞬间/矛盾复盘」由大卡片改为**分段控制器(横格排列)**,选中态 bg-surface+阴影。
9. ✅ **验证**:新增 `v3check.mjs`(10 项:V3.0/时光标签/关系分段/分类 popover/资产固定/分析 sticky/憨憨封面);更新 `navcheck`(分段控制器)、`bookcheck`(资产 absolute 固定)。修复 PetPage hooks 顺序(封面 useState/useRef 上移到条件 return 前,消除 React #310)。本地 28 组 / 线上 28 组全过。

### 10.17 全局滚动根修/指标卡吸顶/分析页固定范围/封面底部(2026-08-08 · V3.1)

> ✅ **已实施完成**并部署上线。根因:body 未禁止垂直滚动,页面内容溢出带动整个界面(含底部导航/侧边导航)一起上滑。本次全局根修 + 按用户指示精确收敛。

1. ✅ **全局禁止 body 滚动(根治「整页上滑带动导航」)**:`index.css` html/body/#root 统一 `overflow: hidden`;App 壳改 `absolute inset-0 overflow-hidden`——所有滚动只发生在壳内独立滚动容器,底部 TabBar / 各页侧边导航(小账/时光/空间)永不被内容带起;导入导出/分类/资产/我呀/小窝等页面同步受益。
2. ✅ **指标卡一律 sticky 吸顶、紧贴状态栏、不留空位**:小账首页(标题+净资产卡)、账单页(本月概览+分栏)、资产页(净资产卡)、倒数日页(标题+期待/足迹切换)全部 `sticky top-0` + 安全区 pt,去除会裁掉顶部字体的 `-mt-4` 负边距、去除占位空 div——指标卡紧贴状态栏固定在顶部,下方内容滚动经过。
3. ✅ **分析页只固定三块**:月份筛选 + 收支三卡 + 消费趋势(近 6 月…);分类占比、月度报告、消费详情全部恢复可滚动(此前误固定过多)。
4. ✅ **憨憨相册封面移到页面底部**(删除宠物按钮之后),不再夹在中间。
5. ✅ **验证**:新增 `v3fixcheck.mjs`(8 项:body 不滚动/首页/账单/资产/倒数日 sticky 吸顶、分析页固定范围、封面底部);更新 `v3check`/`bookcheck` 断言(资产 absolute → sticky)。本地 30 组 / 线上 29 组全过。

### 10.2 其他已知事项

- ⚠️ 原 PAT 曾明文出现于交接对话,须吊销重生成(若仍有效)。
- `xlsx` 包存在 npm 安全公告,导入功能依赖,升级或替换需评估。
- 交接环境默认屏蔽 github.com;源码已在本工作区齐备。

---

## 11. 关键文件索引

| 文件 | 作用 |
|------|------|
| `src/pages/book/BookPage.tsx` | 小账主导航:记账表单、分类网格、图片附件、咔皮导入导出 |
| `src/pages/book/CapturePage.tsx` | 一键拾光:剪贴板读取、预览弹窗、字段编辑、保存 |
| `src/services/captureParser.ts` | OCR 解析器(金额/商户/时间/账户/置信度) |
| `src/services/captureClipboard.ts` | `TITIA_CAPTURE::` 前缀读写 + 防重 |
| `src/stores/useBookStore.ts` | 记账规则、学习闭环、applyBalance |
| `src/stores/useAccountStore.ts` / `useFinanceStore.ts` / `useBudgetStore.ts` / `useCategoryStore.ts` | 账户/财务/预算/分类状态 |
| `src/services/vault.ts` + `useVaultStore.ts` + `VaultPage.tsx` | 密码箱加密与解锁 |
| `src/services/webdav.ts` | Supabase 云同步(当前阶段拟停用) |
| `src/pages/mine/MinePage.tsx` | 我的页:Banner 固定标题、云同步入口、设置 |
| `src/db/schema.ts` | Dexie 表结构 |
| `scripts/e2e/vaultdismiss.mjs` | 密码箱「无法解密」提示可关闭专项回归(16 项) |
| `scripts/e2e/*.mjs` + `run-all.sh` | 全量回归测试 |

---

*本总结仅记录项目状态,未对代码做任何修改。后续如需实施 §10.1 小账模块优化,请基于最新需求文档增量开发。*
