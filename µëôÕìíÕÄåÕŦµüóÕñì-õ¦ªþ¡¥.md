# 打卡历史恢复（dc763 → bj9）

## 问题原因
打卡记录原来存在 `localStorage('titia.appCheckinDays')`，按域名（origin）隔离。
从 dc763 换到 bj9 是两个不同的域名，localStorage 不互通 → 连续打卡历史直接清零。
现已把打卡数据迁到 IndexedDB（和其它数据一致），并修复迁移工具，之后换链接也能带走。

## 恢复步骤（一次性）
1. 用 iPhone Safari 打开**旧链接 dc763**：
   `https://dc763d1c7f1941f1b3d6de42038020c6.bj2.agentos-app.net`
2. 把下面这段「导出书签」代码，新建一个 Safari 书签，把书签的网址改成这段代码
   （长按书签 → 编辑 → 地址栏整段粘贴替换）。
3. 在 dc763 页面里点这个书签 → 它会把本地数据（含打卡）复制到剪贴板，弹窗提示成功。
4. 打开**新链接 bj9**：
   `https://9201b66be0854ef2a3da9f46357cf3cf.bj9.agentos-app.net`
5. 进入「我的」→「数据管理」→「从剪贴板导入（旧链接 dc763 迁移用）」→ 打卡历史即恢复。

> 说明：dc763 是旧代码，打卡只在它的 localStorage 里，标准「导出备份」文件带不出打卡，
> 所以必须用这个书签导出。bj9 已修复，以后打卡就在 IndexedDB，标准备份/书签都能带走。
> 导入会自动先备份当前数据，安全。导入后若主题被旧设置覆盖，去「主题中心」重选即可。

## 导出书签代码（整段复制）
```
javascript:(function(){var TABLES=['records','pets','petHealth','people','todos','shopping','financeItems','cycles','vaultMeta','vaultItems','countdownEvents','transactions','rules','accounts','categories','budgets','customSkins','presetSkins','teaNews','teaFav'];var SKIP=['media'];function getAll(db,name){return new Promise(function(res){try{var tx=db.transaction(name,'readonly');var os=tx.objectStore(name);var rq=os.getAll();rq.onsuccess=function(){res(rq.result||[])};rq.onerror=function(){res([])}}catch(e){res([])}})}var out={version:1,exportedAt:Date.now(),tables:{}};var req=indexedDB.open('titia');req.onsuccess=function(){var db=req.result;var names=Array.from(db.objectStoreNames);var chain=Promise.resolve();TABLES.filter(function(n){return names.indexOf(n)>=0&&SKIP.indexOf(n)<0}).forEach(function(n){chain=chain.then(function(){return getAll(db,n)}).then(function(rows){out.tables[n]=rows})});chain.then(function(){try{var raw=localStorage.getItem('titia.appCheckinDays');if(raw){var arr=JSON.parse(raw);if(Array.isArray(arr)){out.tables.checkin=arr.filter(function(s){return typeof s==='string'}).map(function(d){return{date:d}})}}}catch(e){}var txt=JSON.stringify(out);navigator.clipboard.writeText(txt).then(function(){alert('已复制 '+Object.keys(out.tables).length+' 类数据到剪贴板，去 bj9 点「从剪贴板导入」')},function(){var ta=prompt('复制失败，请长按全选复制下面全部内容：',txt);if(ta!==null){try{navigator.clipboard.writeText(ta)}catch(e){}}})})};req.onerror=function(){alert('无法打开本地数据库 titia')}})();
```
